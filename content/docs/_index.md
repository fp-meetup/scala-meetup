+++
title = "Intro"
weight = 1
+++

This is the main page under the "docs" section of the site. The docs directory have a sub-directory called "compose" which contains documentation about the "Compose" theme.

A little Scala code snippet to show syntax highlighting:
```scala
def factorial(n: Int): Int = {
  if (n == 0) 1
  else n * factorial(n - 1)
}
```